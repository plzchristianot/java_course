import java.util.Date;
import java.sql.*;

public class NamingConflicts {
    Date date;
    // some more code

    //java.util.Date date; These are used when we want to use multiple packages with the same name, we can call them directly and use them to declare a variable
    //java.sql.Date sqlDate;
}
